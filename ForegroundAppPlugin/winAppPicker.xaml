<Window x:Class="ForegroundAppPlugin.winAppPicker"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose applications" Height="520" Width="520"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#F5F5F5">
    <Window.Resources>
        <Style x:Key="BtnPrimary" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#0078D4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnSubtle" TargetType="Button" BasedOn="{StaticResource BtnPrimary}">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#323130"/>
            <Setter Property="BorderBrush" Value="#E1DFDD"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TextBlock_Hint" Grid.Row="0" Foreground="#605E5C" FontSize="12"
                   TextWrapping="Wrap" Margin="0,0,0,10"
                   Text="Pick one or more apps to add to the list."/>

        <TextBox x:Name="TextBox_Search" Grid.Row="1" Height="30" Margin="0,0,0,10"
                 Background="#FFFFFF" Foreground="#323130"
                 BorderBrush="#E1DFDD" BorderThickness="1" Padding="8,4"
                 VerticalContentAlignment="Center"
                 TextChanged="TextBox_Search_TextChanged"/>

        <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#E1DFDD" BorderThickness="1" CornerRadius="4">
            <ListBox x:Name="ListBox_Apps" BorderThickness="0" Background="Transparent"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     SelectionMode="Extended">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="2,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0"
                                      IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding ProcessName}" Foreground="#323130" FontSize="13"/>
                                <TextBlock Text="{Binding DisplayName}" Foreground="#8A8886" FontSize="11"
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <Border Grid.Column="2" VerticalAlignment="Center" Margin="8,0,4,0"
                                    Background="{Binding TagBackground}" CornerRadius="3" Padding="6,2">
                                <TextBlock Text="{Binding TagText}" FontSize="10" Foreground="{Binding TagForeground}"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <TextBlock x:Name="TextBlock_Empty" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="#8A8886" FontSize="12" Visibility="Collapsed"
                   Text="No applications found."/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="Button_Cancel" Content="Cancel" Width="100" Height="32" Margin="0,0,8,0"
                    Style="{StaticResource BtnSubtle}" Click="Button_Cancel_Click"/>
            <Button x:Name="Button_Add" Content="Add selected" Width="120" Height="32"
                    Style="{StaticResource BtnPrimary}" Click="Button_Add_Click"/>
        </StackPanel>
    </Grid>
</Window>
