<Window x:Class="TerminalPlugin.winCommandConfirm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Terminal - Command Confirmation"
        Height="320" Width="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        MinHeight="250" MinWidth="400"
        Background="#F5F5F5">
    <Window.Resources>
        <Style x:Key="BtnPrimary" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#0078D4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="BtnDanger" TargetType="Button">
            <Setter Property="Background" Value="#D13438"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#D13438"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C42B2F"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#A52528"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="&#x26A0;" FontSize="20" Foreground="#FF8C00" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock x:Name="txtTitle"
                       Text="AI requests to execute the following command:"
                       FontSize="16"
                       Foreground="#323130"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Shell Info -->
        <TextBlock x:Name="txtShellInfo"
                   Grid.Row="1"
                   Text="Shell: PowerShell"
                   Foreground="#605E5C"
                   FontSize="12"
                   Margin="0,0,0,10"/>

        <!-- 高危告警：只有安全评估判定为 Dangerous 时才显示 -->
        <Border x:Name="borderDanger"
                Grid.Row="2"
                Background="#FDE7E9"
                BorderBrush="#D13438"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12"
                Margin="0,0,0,10"
                Visibility="Collapsed">
            <StackPanel>
                <TextBlock x:Name="txtDangerTitle"
                           Text="High-risk command"
                           FontSize="13"
                           FontWeight="Bold"
                           Foreground="#D13438"/>
                <TextBlock x:Name="txtDangerReason"
                           FontSize="12"
                           Foreground="#323130"
                           TextWrapping="Wrap"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Command Input -->
        <Border Grid.Row="3"
                Background="#FFFFFF"
                BorderBrush="#E1DFDD"
                BorderThickness="1"
                CornerRadius="8"
                Margin="0,0,0,10">
            <TextBox x:Name="txtCommand"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     FontFamily="Consolas, Courier New, monospace"
                     FontSize="14"
                     Background="Transparent"
                     Foreground="#0078D4"
                     BorderThickness="0"
                     Padding="10"/>
        </Border>

        <!-- Warning -->
        <TextBlock x:Name="txtWarning"
                   Grid.Row="4"
                   Text="Please verify the command before execution. You can modify it if needed."
                   Foreground="#D13438"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnExecute"
                    Content="Execute"
                    Width="100" Height="35"
                    Margin="0,0,10,0"
                    Click="BtnExecute_Click"
                    Style="{StaticResource BtnPrimary}"/>
            <Button x:Name="btnCancel"
                    Content="Cancel"
                    Width="100" Height="35"
                    Click="BtnCancel_Click"
                    Style="{StaticResource BtnDanger}"/>
        </StackPanel>
    </Grid>
</Window>
