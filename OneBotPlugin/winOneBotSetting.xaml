<Window x:Class="OneBotPlugin.winOneBotSetting"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OneBot Settings"
        Width="800" Height="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F5F5F5">
    <Window.Resources>
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#323130"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,12,0,6"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#605E5C"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,4,8,4"/>
        </Style>
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#323130"/>
            <Setter Property="BorderBrush" Value="#E1DFDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#323130"/>
            <Setter Property="BorderBrush" Value="#E1DFDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,2"/>
            <Style.Resources>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Background" Value="#FFFFFF"/>
                    <Setter Property="Foreground" Value="#323130"/>
                    <Setter Property="Padding" Value="8,4"/>
                    <Style.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Background" Value="#0078D4"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Style.Resources>
        </Style>
        <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#323130"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,4"/>
        </Style>
        <Style x:Key="BtnPrimary" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#0078D4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="BtnSecondary" TargetType="Button">
            <Setter Property="Background" Value="#F3F2F1"/>
            <Setter Property="Foreground" Value="#323130"/>
            <Setter Property="BorderBrush" Value="#E1DFDD"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EDEBE9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E1DFDD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Style="{StaticResource SectionHeader}" x:Name="lblNodeManagement" Text="节点管理"/>

                <StackPanel Orientation="Horizontal" Margin="0,8,0,12">
                    <Button x:Name="btnAddNode" Content="添加节点" Click="BtnAddNode_Click" Style="{StaticResource BtnSecondary}"/>
                    <Button x:Name="btnRemoveNode" Content="删除选中节点" Click="BtnRemoveNode_Click" Margin="10,0,0,0" Style="{StaticResource BtnSecondary}"/>
                </StackPanel>

                <ListView x:Name="ListView_Nodes" Height="200" Margin="0,0,0,12" SelectionChanged="ListView_Nodes_SelectionChanged">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="启用" Width="60">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding Enabled}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="节点名称" Width="120" DisplayMemberBinding="{Binding Name}"/>
                            <GridViewColumn Header="类型" Width="120" DisplayMemberBinding="{Binding TypeDisplay}"/>
                            <GridViewColumn Header="URL/Host" Width="180" DisplayMemberBinding="{Binding DisplayUrlOrHost}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <StackPanel x:Name="Panel_NodeDetails" Margin="0,0,0,16">
                    <TextBlock Style="{StaticResource SectionHeader}" x:Name="lblNodeConfig" Text="节点配置"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="节点名称:"/>
                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtNodeName" Style="{StaticResource InputStyle}" TextChanged="TxtNodeName_TextChanged"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="节点类型:"/>
                        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="cmbNodeType" Style="{StaticResource ComboBoxStyle}" SelectionChanged="CmbNodeType_SelectionChanged">
                            <ComboBoxItem Content="正向 WebSocket" Tag="ForwardWS"/>
                            <ComboBoxItem Content="反向 WebSocket" Tag="ReverseWS"/>
                            <ComboBoxItem Content="HTTP API" Tag="HttpApi"/>
                        </ComboBox>

                        <TextBlock Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}" x:Name="lblUrl" Text="URL:"/>
                        <TextBox Grid.Row="2" Grid.Column="1" x:Name="txtUrl" Style="{StaticResource InputStyle}" TextChanged="TxtUrl_TextChanged"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Style="{StaticResource LabelStyle}" x:Name="lblHost" Text="Host:" Visibility="Collapsed"/>
                        <Grid Grid.Row="3" Grid.Column="1" x:Name="Grid_ReverseWS" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" x:Name="txtHost" Style="{StaticResource InputStyle}" TextChanged="TxtHost_TextChanged"/>
                            <TextBlock Grid.Column="1" Style="{StaticResource LabelStyle}" Text="Port:" Margin="8,4,0,4"/>
                            <TextBox Grid.Column="2" x:Name="txtPort" Style="{StaticResource InputStyle}" Width="80" TextChanged="TxtPort_TextChanged"/>
                        </Grid>

                        <TextBlock Grid.Row="4" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="Access Token:"/>
                        <TextBox Grid.Row="4" Grid.Column="1" x:Name="txtAccessToken" Style="{StaticResource InputStyle}" TextChanged="TxtAccessToken_TextChanged"/>
                    </Grid>
                </StackPanel>

                <TextBlock Style="{StaticResource SectionHeader}" x:Name="lblAuth" Text="认证与权限"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="Bot QQ:"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtBotQQ" Style="{StaticResource InputStyle}"/>
                    <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource LabelStyle}" x:Name="lblMasterQQ" Text="Master QQ:"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtMasterQQ" Style="{StaticResource InputStyle}"/>
                </Grid>

                <TextBlock Style="{StaticResource SectionHeader}" x:Name="lblFilter" Text="消息过滤"/>
                <CheckBox x:Name="chkAllowAllPrivate" Style="{StaticResource CheckBoxStyle}" Content="允许所有私聊消息"/>
                <CheckBox x:Name="chkAllowAllGroup" Style="{StaticResource CheckBoxStyle}" Content="允许所有群消息"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}" x:Name="lblAllowedUsers" Text="用户白名单:"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtAllowedUsers" Style="{StaticResource InputStyle}"/>
                    <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource LabelStyle}" x:Name="lblAllowedGroups" Text="群白名单:"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtAllowedGroups" Style="{StaticResource InputStyle}"/>
                </Grid>

                <TextBlock Style="{StaticResource SectionHeader}" x:Name="lblBehavior" Text="行为"/>
                <CheckBox x:Name="chkAtTriggerOnly" Style="{StaticResource CheckBoxStyle}" Content="群聊：仅 @机器人 时响应"/>
                <CheckBox x:Name="chkReplyWithAt" Style="{StaticResource CheckBoxStyle}" Content="群聊：回复时 @发送者"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="btnCancel" Style="{StaticResource BtnSecondary}" Content="取消" Click="BtnCancel_Click" Margin="0,0,10,0"/>
            <Button x:Name="btnSave" Style="{StaticResource BtnPrimary}" Content="保存" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
